<?xml version="1.0" encoding="utf-8"?>
<!--
  Dev-only cleartext policy: allows ws:// and http:// to private LAN addresses
  (192.168.x.x, 10.x.x.x, 172.16-31.x.x) and localhost.
  Production builds should use wss:// / https:// and this file should be
  replaced with one that omits the <domain-config> blocks.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- Local dev server accessed from a physical device on the same LAN -->
        <domain includeSubdomains="false">192.168.100.65</domain>
        <domain includeSubdomains="false">192.168.100.2</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
